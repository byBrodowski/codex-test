<Window x:Class="PremiumDock.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PremiumDock"
        Title="Premium Dock"
        Height="120"
        Width="720"
        AllowsTransparency="True"
        WindowStyle="None"
        Background="Transparent"
        Topmost="True"
        ShowInTaskbar="False">
  <Window.Resources>
    <DropShadowEffect x:Key="PanelShadow"
                      BlurRadius="24"
                      ShadowDepth="0"
                      Opacity="0.35"
                      Color="#4D5A7A" />
    <Style TargetType="Button" x:Key="IconButton">
      <Setter Property="Background" Value="Transparent" />
      <Setter Property="BorderThickness" Value="0" />
      <Setter Property="Padding" Value="8" />
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border CornerRadius="12" Background="Transparent">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="#11FFFFFF" />
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Window.Resources>

  <Border Margin="16"
          CornerRadius="24"
          Background="{StaticResource PanelGradient}"
          Opacity="0.98"
          Effect="{StaticResource PanelShadow}">
    <Border BorderThickness="1" BorderBrush="#40FFFFFF" CornerRadius="24">
      <Grid Margin="16">
        <StackPanel>
          <TextBlock Text="Premium Dock" FontSize="16" FontWeight="SemiBold" Foreground="#334155" />
          <TextBlock Text="Перетягніть сюди іконки або ярлики" FontSize="12" Foreground="#64748B" Margin="0,4,0,12" />
          <ItemsControl ItemsSource="{Binding DockItems}">
            <ItemsControl.ItemsPanel>
              <ItemsPanelTemplate>
                <WrapPanel />
              </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
              <DataTemplate DataType="{x:Type local:DockItem}">
                <Button Style="{StaticResource IconButton}" ToolTip="{Binding DisplayName}" Click="OnDockItemClick">
                  <StackPanel Width="72" HorizontalAlignment="Center">
                    <Border Width="48" Height="48" CornerRadius="12" Background="#F8FAFC">
                      <Image Width="32" Height="32" Source="{Binding Icon}" Stretch="Uniform" />
                    </Border>
                    <TextBlock Text="{Binding DisplayName}"
                               FontSize="11"
                               Foreground="#475569"
                               TextAlignment="Center"
                               TextTrimming="CharacterEllipsis"
                               Margin="0,6,0,0" />
                  </StackPanel>
                </Button>
              </DataTemplate>
            </ItemsControl.ItemTemplate>
          </ItemsControl>
        </StackPanel>
      </Grid>
    </Border>
  </Border>
</Window>
